extends ../base

block content 
    if errorMessage 
        span=errorMessage
    form(method="POST")
        input(name="old", placeholder="Old password", type="password", required)
        input(name="newPassword", placeholder="New password", type="password", required)
        input(name="newPassword2", placeholder="New password confirmaiton", type="password", required)
        input(type="submit", value="Change password")