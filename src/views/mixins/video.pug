mixin video(video)
    div
        h4
            a(href=`/videos/${video.id}`) #{video.title}
        p #{video.description}
        small #{video.createdAt}
        ul
            each tag in video.hashtags
                li=tag
        hr